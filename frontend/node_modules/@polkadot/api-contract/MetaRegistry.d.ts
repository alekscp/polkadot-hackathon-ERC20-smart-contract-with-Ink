import { ChainProperties, SiLookupTypeId, SiType } from '@polkadot/types/interfaces';
import { TypeDef } from '@polkadot/types/types';
import { TypeRegistry } from '@polkadot/types';
export declare function getRegistryOffset(id: SiLookupTypeId): number;
export default class MetaRegistry extends TypeRegistry {
    #private;
    readonly metaTypeDefs: TypeDef[];
    constructor(chainProperties?: ChainProperties);
    setMetaTypes(metaTypes: SiType[]): void;
    getMetaTypeDef(id: SiLookupTypeId): TypeDef;
}
